name: Deploy via FTP (Hostinger)

on:
  push:
    branches:
      - n8nlfl_claude
      - claude/landing-page-evaluation-fasl71
  workflow_dispatch:

concurrency:
  group: ftp-deploy
  cancel-in-progress: true

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

     
        with:
          server: 212.85.6.164
          username: u899095009
          password: Na@0896!
          port: 21
          protocol: ftp
          local-dir: ./
          server-dir: public_html/
          # Não enviar arquivos internos do repositório para o servidor:
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/.devcontainer/**
            **/docs/**
            **/CNAME
            **/*.md
            **/img/LEIA-ME.txt
            **/data/config.php
            **/uploads/img_*
